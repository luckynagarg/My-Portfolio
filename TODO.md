# TODO - Make Portfolio Good

- [ ] Fix Back-to-top link target by adding `id="top"` (or equivalent) in `index.html`.
- [ ] Replace placeholder `Live Demo` links (`href="#"`) with non-dead behavior (use `data-demo` + JS to hide/disable if no URL; optionally link if present).
- [ ] Add reduced-motion support for preloader, AOS/Typed/counter animations (in CSS/JS) to respect `prefers-reduced-motion`.
- [ ] Improve performance: add `loading="lazy"`/`decoding="async"` to non-critical images and add basic dimensions where safe.
- [ ] Ensure resume button doesn’t break: keep existing link if file exists; otherwise hide it via JS fallback.
- [ ] Quick manual test checklist (in browser) after changes.


